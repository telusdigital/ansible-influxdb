---
driver:
  name: docker
  privileged: true

provisioner:
  name: default.yml
  hosts: test-kitchen
  requirements_path: requirements.yml

platforms:
  - name: ubuntu-14.04
suites:
  - name: default
